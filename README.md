# EnglishForMom

EnglishForMom is an Expo and React Native application for learning practical everyday English, designed for a Romanian-speaking adult beginner.

## Development

Install dependencies and start the Expo development server:

```bash
npm install
npm start
```

Run the available checks with:

```bash
npm run lint
npm run typecheck
```
